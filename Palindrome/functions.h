#include <string>
#include <iostream>
#include <cstring>
#include <cctype>
#include <vector>


using namespace std;

//function declaration for all functions used in functions.cpp and pal.cpp

bool isPal (string myString, bool flag_case, bool flage_spaces);

void printUsageInfo (string executable);

bool isC (string cmmdline);

bool isS (string cmmdline);

bool isQ (char cmmdline);

